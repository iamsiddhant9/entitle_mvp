# Django resolves to 4.2 LTS on Python 3.9 (local dev) and 5.x on Python >= 3.10
# (Docker image uses python:3.12). The codebase is compatible with both.
Django>=4.2,<6.0
djangorestframework>=3.15
django-cors-headers>=4.3
dj-database-url>=2.1
psycopg2-binary>=2.9; python_version >= "3.10"
python-dotenv>=1.0

# Document pipeline (blur detection + image handling)
Pillow>=10.0
numpy>=1.24

# AI explanations & document extraction (optional at runtime — graceful fallback)
google-genai>=1.0; python_version >= "3.10"

# Blockchain anchoring (optional at runtime — simulated mode when unconfigured)
web3>=6.15
qrcode>=7.4
